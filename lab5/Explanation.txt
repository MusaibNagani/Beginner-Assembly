lab5a.asm - Description
This assembly program generates random uppercase strings of 10 characters each. The program initializes a loop to run 20 times, calling the genString procedure in each iteration. Within genString, the program generates a random number between 0 and 25 using the RandomRange procedure, which corresponds to a lowercase letter. By adding 65 to this value, the program converts the random number to an uppercase ASCII letter, which is then stored in an array and printed to the console using WriteChar. The process repeats until 10 characters are generated, after which a newline is printed, and the program continues to generate the next string until the loop completes 20 iterations.

lab5b.asm - Description
This assembly program is a partial implementation that seems to be intended to manipulate text color and output characters in a loop. The program initializes some registers and variables, including num, which is set to 16, and then enters a loop labeled foreground. Within this loop, another loop labeled background is initiated. The SetTextColor procedure is called, likely to change the color of the text output. The program then moves the character 'z' into the EAX register and outputs it using the WriteChar procedure. Afterward, the program modifies the value in EAX by adding 10h before completing the loop. However, the program appears incomplete, as it lacks a clear exit condition or a full implementation of the intended functionality.